git add .
git commit -m "Updated"
git push origin main

帮我解决前端问题，我在做一个【youtube summary】的工具，我用next.js和tailwind，目前在还原figma里的设计，你能参考导出的figma设计的图片，还原里的布局，交互逻辑，功能板块的划分，组件

你帮我你能不能把这里的功能改成,给我相对应新的代码：
1.输入框的高度是10和gobutton保持一直，并上下是居中的
1. 用户在输入框输入文字前，gobutton不可交互（变灰）
2. 输入文字并检测是个youtube链接后，gobutton变得可点击（按钮背景变浅的主题色）
3. 用户鼠标hover后，请求服务器，gobutton的按钮背景变成主题色
4. 输入框左边预留一个新的空间，在这个空间（输入框左边）新增一个上传文件的按钮，颜色是主题色，svg文件是和arrow.png同文件夹里的upload.svg

现在的代码：

// src/app/components/GoButton.js
import React from 'react';

const GoButton = ({ onClick }) => {
  return (
    <div
      onClick={onClick}
      className="relative w-10 h-10 cursor-pointer rounded-full border-2 border-teal-500 bg-white flex items-center justify-center transition-colors duration-300 hover:bg-teal-500 group"
    >
      <svg
        className="w-8 h-8 text-teal-500 transition-colors duration-300 group-hover:text-white"
        fill="none"
        stroke="currentColor"
        strokeWidth="1.5"
        viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          strokeLinecap="round"
          strokeLinejoin="round"
          d="M5 12h14M12 5l7 7-7 7"
        ></path>
      </svg>
    </div>
  );
};

export default GoButton;
